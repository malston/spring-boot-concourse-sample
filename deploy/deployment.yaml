apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: spring-boot-concourse-example
  name: spring-boot-concourse-example
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-boot-concourse-example
  template:
    metadata:
      labels:
        app: spring-boot-concourse-example
    spec:
      containers:
      - image: "alexbrand/spring-boot-concourse-example:__IMAGE_TAG__"
        name: spring-boot-concourse-example
